[pytest]
testpaths = tests
norecursedirs = venv .venv __pycache__ .git .tox dist build *.egg-info
log_cli = true
log_cli_level = DEBUG
markers =
    positive: happy path tests
    negative: error and invalid input tests
    edge_case: boundary and corner case tests